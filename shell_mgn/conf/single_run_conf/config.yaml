hydra:
    run:
        dir: ./shell_mgn/testing/multi_head_without_gradnorm/single-subcase-min-max-tanh/outputs
    job:
        chdir: True

ckpt_path: ./shell_mgn/testing/multi_head_without_gradnorm/single-subcase-min-max-tanh/checkpoints
ckpt_name: ./shell.pt
data_path: /home/sces213/Malaika/lts_modulus/shell_mgn/dataset/dataset.hdf5 # -> path to hdf5
results_dir: ./shell_mgn/testing/multi_head_without_gradnorm/single-subcase-min-max-tanh/results

input_dim_nodes: 11 # 3d pos, 1d ntype, 1d thickness, 3d spc 3d force | ndata -> pos(2d) ,markers 5 ; not sure
input_dim_edges: 5 # 3d disp, 1d norm, 1d etypes | edata -> disp_vector(2d) , disp_norm
output_dim: 1 # disp_x,y,z | u,v (velocities in x and y directions), p pressure
hidden_dim_node_encoder: 128
hidden_dim_edge_encoder: 128
hidden_dim_node_decoder: 128

aggregation: "sum"
batch_size: 1
epochs: 100

num_training_samples: 500
num_validation_samples: 10
num_test_samples: 10

lr: 1e-4

amp: False
jit: False
wandb_mode: "online"

## seeing if it could serve as a template
## loss and normalization is accessed as in sweep.yaml, can't remove from here that says not defined in config


loss:
    L1Loss

normalization:
    min-max
